{
  "manifest_version": "0.4",
  "name": "lit-lake",
  "version": "0.1.0",
  "description": "A MCP to connect your reference managers to Claude",
  "author": {
    "name": "Elliot Roe",
    "url": "https://github.com/ElliotRoe"
  },
  "server": {
    "type": "uv",
    "entry_point": "src/litlake/server.py",
    "mcp_config": {
      "command": "uv",
      "args": ["run", "lit-lake-mcp"],
      "env": {
        "EMBED_DISABLED": "${user_config.disable_embedding}",
        "ZOTERO_DB_PATH": "${user_config.z_db}",
        "LIT_LAKE_PATH": "${user_config.lit_lake_folder}",
        "GEMINI_API_KEY": "${user_config.gemini_api_key}"
      }
    }
  },
  "user_config": {
    "disable_embedding": {
      "type": "boolean",
      "title": "Disable Embedding",
      "description": "Disable embedding background worker to save resources.",
      "required": false,
      "sensitive": false
    },
    "z_db": {
      "type": "file",
      "title": "Zotero DB",
      "description": "Path to Zotero sqlite DB (optional: defaults to ~/Zotero/zotero.sqlite)",
      "required": false,
      "sensitive": false
    },
    "lit_lake_folder": {
      "type": "directory",
      "title": "Lit Lake Folder",
      "description": "Parent folder where LitLake data directory will be created.",
      "required": false,
      "sensitive": false
    },
    "gemini_api_key": {
      "type": "string",
      "title": "Gemini API Key",
      "description": "Optional API key for Gemini PDF conversion. If omitted, local PDF conversion is used.",
      "required": false,
      "sensitive": true
    }
  },
  "license": "MIT"
}
